{"key":"n_auth_flow","title":"API Authentication Flow","content":"Two auth methods supported: 1) Cookie-based (web clients): login → receive httpOnly cookie with session ID → session validated on each request via auth-guard. 2) Bearer token (API clients): login → receive JWT access + refresh tokens → access token in Authorization header. Both methods share the same permission model. API keys for machine-to-machine communication use separate validation path.","tags":["auth","api","security"]}
